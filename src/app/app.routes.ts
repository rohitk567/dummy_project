import { Routes } from '@angular/router';
import { FileExplorerComponent } from './components/file-explorer/file-explorer.component';

export const routes: Routes = [{ path: '', component: FileExplorerComponent }];
